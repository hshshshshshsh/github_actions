name: Demo Pipeline

on:
  push:
    branches:
      - nodeapp

jobs:
  demo:
    runs-on: linux

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Print details
        run: echo "Code checkout successful"
