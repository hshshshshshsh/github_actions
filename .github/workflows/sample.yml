name: Demo Pipeline

on:
  push:
    branches:
      - nodeapp

jobs:
  demo:
    runs-on: [githubactions, self-hosted, Linux, X64]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Print details
        run: echo "Code checkout successful"
